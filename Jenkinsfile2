pipeline {
    agent any
    environment {
        BASE_DIR = "/home/unitekserver/projects/web-projects"
    }
    stages {
        stage('Add XAMPP VirtualHost') {
            steps {
                script {
                    echo "🔹 Adding XAMPP VirtualHost"
                    sh "bash $BASE_DIR/bash/add-xampp-vhost.sh"
                }
            }
        }
    }
}
